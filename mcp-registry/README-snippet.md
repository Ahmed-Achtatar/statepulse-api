## MCP Server

StatePulse API is available as a remote MCP server — point any MCP client at:

```
https://statepulse-api.hahavoid0.workers.dev/mcp
```

- Transport: streamable HTTP
- Auth: none (paid tools settle via x402 USDC on Base, per call)
- Tools exposed: 50
- Metadata: https://statepulse-api.hahavoid0.workers.dev/.well-known/mcp.json

### GitHub topics to set
`mcp` `mcp-server` `model-context-protocol` `x402` `ai-agents` `usdc` `base` `pay-per-call` `cloudflare-workers`

```bash
gh repo edit --add-topic mcp --add-topic mcp-server --add-topic model-context-protocol --add-topic x402 --add-topic ai-agents --add-topic usdc --add-topic base --add-topic pay-per-call --add-topic cloudflare-workers
```
