# Burner wallet that pays per call (USDC on Base). NEVER use a primary wallet.
EVM_PRIVATE_KEY=0xYOUR_BURNER_PRIVATE_KEY

# Optional override (defaults to the live StatePulse Worker)
# STATEPULSE_BASE_URL=https://statepulse-api.hahavoid0.workers.dev
